A tool for video action recognition, primarily for detecting scratching events in videos of mice. A cropping tool is provided to automatically track and crop objects from videos using segment anything model 2 (SAM2).

The computer vision model for video action recognition is designed for low resource datasets and achieves high accuracy and fast training on datasets of the order of hundreds of events, reducing the need to annotate large datasets. Applications include mouse activity tracking and live cell culture automated analysis.